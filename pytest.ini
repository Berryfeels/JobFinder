[pytest]
testpaths = tests
markers =
    unit: Unit tests for pure functions and small components
    integration: Integration tests across multiple modules or DB
    e2e: End-to-end flows across fetchers -> normalizer -> DB
    edge: Edge case tests and error handling
